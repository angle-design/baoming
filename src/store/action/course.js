import * as TYPES from '../action-types';
import {logoList} from '../../api/course';
let course={
//   获取机构列表
// queryLogo(cid,p=1) {
//     return async dispatch => {
//         let result = await logoList(cid,p);
//         dispatch({
//             type: TYPES.COURSE_LOGO_LIST,
//             result
//         });
//     }
// },
}
export default course;